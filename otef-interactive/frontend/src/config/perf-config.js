export const PERF_CONFIG = Object.freeze({
  map: Object.freeze({
    enablePreferCanvas: true,
    enableRafViewportApply: true,
    minApplyIntervalMs: 33,
    animateRemoteViewport: false,
  }),
  projection: Object.freeze({
    animationMaxFps: 30,
  }),
});
