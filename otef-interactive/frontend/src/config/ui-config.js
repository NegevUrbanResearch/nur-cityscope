export const UI_CONFIG = Object.freeze({
  curatedPalette: Object.freeze([
    "#ff6b35",
    "#00d4ff",
    "#4caf50",
    "#ffd166",
    "#ef476f",
  ]),
  legend: Object.freeze({
    fallbackLandUseField: "KVUZ_TRG",
    fallbackScheme: "category10",
  }),
});
